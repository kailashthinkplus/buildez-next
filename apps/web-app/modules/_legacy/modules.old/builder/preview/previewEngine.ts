import { PageBlueprint } from "@/modules/blueprint/types";
import { Device } from "./previewTypes";

export function generatePreviewTree(
  page: PageBlueprint,
  device: Device
): any {
  return page;
}
