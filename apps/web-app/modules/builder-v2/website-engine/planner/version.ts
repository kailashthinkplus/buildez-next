/**
 * Immutable version metadata for the inert AI Planner.
 *
 * @example
 * const version = AI_PLANNER_VERSION_STRING;
 */
export const AI_PLANNER_VERSION = Object.freeze({
  module: "planner",
  phase: "PHASE_38_AI_PLANNER",
  version: "38.0.0",
});

/**
 * Stable AI Planner version string.
 *
 * @example
 * const version = AI_PLANNER_VERSION_STRING;
 */
export const AI_PLANNER_VERSION_STRING = AI_PLANNER_VERSION.version;
