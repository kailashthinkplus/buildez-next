# Golden Website Reports

`GoldenWebsiteRunner` returns JSON-safe reports containing scores, warnings, failed rules, selected components, composition trace, and design trace. Benchmark tests keep report generation side-effect free; persistent report artifacts can be added by CI without changing the runner.
