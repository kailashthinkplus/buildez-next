/**
 * Immutable version metadata for ai-v9 Shadow Comparison.
 *
 * @example
 * const version = SHADOW_COMPARISON_VERSION_STRING;
 */
export const SHADOW_COMPARISON_VERSION = Object.freeze({
  module: "shadow-comparison",
  phase: "PHASE_40_AI_V9_SHADOW_COMPARISON",
  version: "40.0.0",
});

/**
 * Stable Shadow Comparison version string.
 *
 * @example
 * const version = SHADOW_COMPARISON_VERSION_STRING;
 */
export const SHADOW_COMPARISON_VERSION_STRING = SHADOW_COMPARISON_VERSION.version;
