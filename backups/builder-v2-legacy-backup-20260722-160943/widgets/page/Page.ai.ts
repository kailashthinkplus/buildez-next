const PageAIPrompt = `
Represents the root page.

Rules:

- Always exactly one Page.
- Page can contain Sections only.
- Never place Heading directly inside Page.
- Never place Button directly inside Page.
`;

export default PageAIPrompt;