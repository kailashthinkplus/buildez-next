import {mkdirSync,writeFileSync} from "node:fs";import {dirname,resolve} from "node:path";import {fileURLToPath} from "node:url";import {PREMIUM_FIXTURES,isApprovedGoldStandard} from "../premium/registry";import {pendingHumanQuality} from "../premium/quality";
const root=resolve(dirname(fileURLToPath(import.meta.url)),"premium-corpus");const fixtures=PREMIUM_FIXTURES.map(f=>({fixtureId:f.fixtureId,category:f.category,industry:f.industry,designGrammar:f.designGrammar,manualApproval:f.approval,sourceDesignQuality:pendingHumanQuality(),contractFidelity:null,compiledVisualFidelity:null,endUserAcceptability:false,genericDesignDiagnostics:[],compilerLosses:[],responsiveLosses:[],mediaLosses:[],screenshotPaths:null}));const summary={classification:"product-quality",engineeringHealthReport:"../m4/corpus-summary.json",approvedCount:PREMIUM_FIXTURES.filter(isApprovedGoldStandard).length,candidateCount:fixtures.length,phasePassed:false,reason:"Human approval and source-design scoring are required before premium certification.",fixtures};mkdirSync(root,{recursive:true});writeFileSync(resolve(root,"premium-corpus-summary.json"),JSON.stringify(summary,null,2)+"\n");writeFileSync(resolve(root,"premium-corpus-summary.md"),`# Premium corpus summary\n\nCandidates: ${fixtures.length}  \nHuman-approved gold standards: ${summary.approvedCount}  \nPhase passed: **no**\n\nNo candidate is auto-approved. Source quality, compiled fidelity, and end-user acceptability remain separate.\n\n| Fixture | Category | Approval | Grammar |\n|---|---|---|---|\n${fixtures.map(f=>`| ${f.fixtureId} | ${f.category} | ${f.manualApproval.state} | ${f.designGrammar} |`).join("\n")}\n`);
