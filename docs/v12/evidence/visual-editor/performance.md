# Performance

Hover does not rerender the Builder shell. Overlay geometry is iframe-local. Source commits are discrete and preview restarts use the existing Vite runtime. Formal latency measurements were not captured because browser authentication blocked the fixture.
