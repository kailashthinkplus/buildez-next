# Pages and routing

The V12 agent prompt now requires a real route, source file, navigation target, and `src/buildez.pages.json` record for each generated page. Interactive create/rename/duplicate/delete synchronization and orphan checking are not implemented.
