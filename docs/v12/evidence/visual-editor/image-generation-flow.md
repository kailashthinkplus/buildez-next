# Image generation flow

V12 now detects underspecified image requests and returns a clarification instead of generating an arbitrary image. Actual provider generation, optimization, R2 persistence, source replacement, and rollback are not complete in this slice.
