# Selection overlay

One iframe-owned, fixed-position overlay system handles hover and selected states without reflow or pointer interception. Bounds refresh on scroll, resize, DOM mutation, and element resize. Deepest instrumented targets are selected; parent selection is exposed through the toolbar. Preview mode clears and hides instrumentation.
