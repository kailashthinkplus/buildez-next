# Test summary

Baseline: root typecheck pass; Builder typecheck pass; V12 boundary tests 7/7 pass; production build has a pre-existing legacy `NodeType` failure.

Added tests cover stable identity, bridge rejection, exact source-backed text/class patches, stale-anchor rollback behavior, and image clarification.

Browser capture was attempted at `http://localhost:3002/app/workspace/websites` and redirected to `/app/login`. Authenticated visual/browser suites were not run.
