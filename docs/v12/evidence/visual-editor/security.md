# Security

Project paths retain existing traversal protection. The bridge validates version, session, direction, origin, and window identity. Browser messages contain project-relative paths only. Source edits allow a fixed attribute list and exact source anchor.
