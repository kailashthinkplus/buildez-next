# 2026-07-12 — Builder RC-T3G Hierarchy and Completion

BRC-0016 was reproduced at the CommandBus boundary and closed. Explicit Container/Column palette targets now create one direct insertion step. Node coverage verifies Heading, Button, and Text into empty Containers, index-zero ordering, validation, undo, redo, and deterministic serialization. The browser fixture now retains a real empty Container lane, uses an unoccluded visible release coordinate, and exposes the final production drop commit. Empty/non-empty/first/last palette scenarios pass with persistence and reload.

The next mandatory gate found BRC-0018: Escape cancellation dirties/autosaves and release over Builder chrome can retain an underlying Section target. Failing authenticated regressions remain in `invalid-dnd.spec.ts`. Per the milestone gate, scroll/zoom/responsive/persistence-matrix/journey/visual expansion stopped. RC-T3 remains failed and RC-T4 was not started.
