// RFC-004 — normalize.ts

import { PageBlueprint, SectionBlueprint } from "../types";

export function normalizePage(page: PageBlueprint): PageBlueprint {
  // TODO (RFC-004 normalization)
  return page;
}

export function normalizeNode(node: SectionBlueprint): SectionBlueprint {
  // TODO
  return node;
}
